# This is a symlink to all the submitted files from
# http://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE73214
/data_files

# This is where the output files go (it's currently a symlink to a
# Dropbox folder)
/results

# This is where intermediate R data files and such generated by the
# analysis pipeline are saved
/saved_data
/sra_files
